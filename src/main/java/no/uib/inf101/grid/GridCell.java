package no.uib.inf101.grid;

public record GridCell<E>(CellPosition pos, E value) {
/**
This file contains a record class called GridCell that represents a cell in a grid. 
It consists of a CellPosition object that specifies the position of the cell in the grid, 
and a generic type E that represents the value stored in the cell.

*/
}
